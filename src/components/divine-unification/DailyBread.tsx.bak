"use client";

import { motion } from "framer-motion";
import { withErrorBoundary } from "@/components/with-error-boundary";
import { cn } from "@/lib/utils";
import { withErrorBoundary } from "@/components/with-error-boundary";

interface DailyBreadProps {
  className?: string;
}

interface BreadOffering {
  type: string;
  blessing: string;
  nourishment: string;
}

const sacredOfferings: BreadOffering[] = [
  {
    type: "Wisdom",
    blessing: "May knowledge become understanding",
    nourishment: "Feeds the mind",
  },
  {
    type: "Courage",
    blessing: "May fear transform to faith",
    nourishment: "Feeds the spirit",
  },
  {
    type: "Love",
    blessing: "May hearts open to receive",
    nourishment: "Feeds the soul",
  },
  {
    type: "Truth",
    blessing: "May clarity pierce illusion",
    nourishment: "Feeds consciousness",
  },
  {
    type: "Service",
    blessing: "May giving become receiving",
    nourishment: "Feeds community",
  },
  {
    type: "Grace",
    blessing: "May forgiveness flow freely",
    nourishment: "Feeds healing",
  },
  {
    type: "Joy",
    blessing: "May laughter light the way",
    nourishment: "Feeds celebration",
  },
];

export function DailyBread({ className }: DailyBreadProps) {
  return (
    <div className={cn("relative w-full max-w-4xl mx-auto", className)}>
      {/* Sacred Loaf */}
      <motion.div
        className="relative mb-12"
        initial={{ scale: 0.8, opacity: 0 }}
        animate={{ scale: 1, opacity: 1 }}
        transition={{ duration: 1.5, ease: "easeOut" }}
      >
        <div className="absolute inset-0 bg-gradient-to-br from-amber-400/30 to-yellow-600/30 blur-3xl" />
        <div className="relative bg-gradient-to-br from-amber-600 to-yellow-700 rounded-3xl p-8 shadow-2xl">
          <h2 className="text-4xl font-bold text-center text-amber-100 mb-4">
            Our Daily Bread
          </h2>
          <p className="text-center text-amber-200 text-lg">
            "I am the bread of life. Whoever comes to me will never go hungry."
          </p>
        </div>
      </motion.div>

      {/* Sacred Slices */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {sacredOfferings.map((offering, index) => (
          <motion.div
            key={offering.type}
            className="relative group"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: index * 0.1 + 0.5 }}
          >
            <div className="absolute inset-0 bg-gradient-to-br from-amber-500/20 to-yellow-500/20 rounded-lg blur-xl group-hover:blur-2xl transition-all duration-300" />
            <motion.div
              className="relative bg-gradient-to-br from-amber-900/40 to-yellow-900/40 backdrop-blur-sm p-6 rounded-lg border border-amber-500/30 hover:border-amber-400/50 transition-all duration-300"
              whileHover={{ scale: 1.05 }}
              transition={{ type: "spring", stiffness: 300 }}
            >
              <h3 className="text-xl font-bold text-amber-300 mb-2">
                {offering.type}
              </h3>
              <p className="text-amber-100 text-sm mb-3 italic">
                "{offering.blessing}"
              </p>
              <div className="pt-3 border-t border-amber-600/30">
                <p className="text-yellow-200 text-xs">
                  {offering.nourishment}
                </p>
              </div>
            </motion.div>
          </motion.div>
        ))}
      </div>

      {/* Breaking Bread Animation */}
      <motion.div
        className="mt-12 text-center"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 1.5 }}
      >
        <motion.div
          animate={{
            scale: [1, 1.1, 1],
            rotate: [0, 5, -5, 0],
          }}
          transition={{
            duration: 4,
            repeat: Infinity,
            ease: "easeInOut",
          }}
          className="inline-block"
        >
          <div className="text-6xl">🍞</div>
        </motion.div>
        <p className="mt-4 text-amber-200 text-lg font-semibold">
          Break • Share • Nourish • Unite
        </p>
      </motion.div>

      {/* Divine Distribution */}
      <motion.div
        className="mt-8 p-6 bg-gradient-to-r from-amber-900/20 via-yellow-900/20 to-amber-900/20 rounded-xl"
        initial={{ opacity: 0, scaleX: 0 }}
        animate={{ opacity: 1, scaleX: 1 }}
        transition={{ delay: 2, duration: 1 }}
      >
        <p className="text-center text-amber-100 text-lg">
          "When we break bread together, we become ONE body. Each piece carries
          the whole, each soul reflects the divine."
        </p>
      </motion.div>
    </div>
  );
}
